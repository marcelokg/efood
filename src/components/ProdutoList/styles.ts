import { styled } from 'styled-components'

export const CardProdutosContainer = styled.main`
  max-width: 1024px;
  width: 100%;
  margin: 0 auto;
`
export const CardProdutosList = styled.ul`
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  column-gap: 32px;
  margin-top: 56px;
  margin-bottom: 120px;
`
